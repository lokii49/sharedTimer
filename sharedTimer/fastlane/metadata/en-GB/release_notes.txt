• New: "Start later" for Sequences — schedule a routine to begin at a future time instead of right away, with a redesigned Scheduled state showing the start time.
• Fixed rows only responding to taps on the label text, not the rest of the row.
• Various reliability fixes.
